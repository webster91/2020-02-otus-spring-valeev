import React from 'react';
import BookList from '../book-list';

const BooksPage = () => {
    return (
        <React.Fragment>
            <BookList/>
        </React.Fragment>
    );
};

export default BooksPage;