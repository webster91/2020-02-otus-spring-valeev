spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
  jpa:
    properties:
      hibernate:
        show_sql: false
    hibernate:
      ddl-auto: none
logging:
  level:
    ROOT: INFO