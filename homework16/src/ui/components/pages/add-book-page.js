import React from 'react';
import BookForm from '../book-form';

const AddBookPage = () => {
    return (
        <React.Fragment>
            <BookForm/>
        </React.Fragment>
    );
};

export default AddBookPage;