spring:
  datasource:
  url: jdbc:mysql://localhost/test
  username: dbuser
  password: dbpassword
  driver-class-name: com.mysql.jdbc.Driver
  initialization-mode: always
  schema: schema.sql
  data: data.sql